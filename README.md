# CI/CD Demo App

A simple Flask application for the SpecterOps Software Supply Chain Security for Red Teamers workshop.

## Endpoints

- `GET /` — Welcome message and status
- `GET /health` — Health check
- `GET /items` — List all items
- `GET /items/<id>` — Get a specific item by ID
